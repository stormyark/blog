import Link from "next/link";

const Header = () => {
  return (
    <footer className="fixed top-0 left-0 right-0 z-50 flex items-center w-full justify-between bg-background/95 backdrop-blur">
      <div className="container mx-auto px-4 py-4 flex items-center justify-between relative border-b border-gray-500"></div>
    </footer>
  );
};

export default Header;
